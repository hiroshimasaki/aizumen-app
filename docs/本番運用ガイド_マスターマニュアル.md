# AiZumen 本番移行・運用ガイド (統合版)

本ドキュメントは、システムリリース前後の手順、データベース変更の運用、および運用開始後の保守・監視体制を一つにまとめた包括的なガイドです。

---

## 1. 事前準備：インフラ・基盤
リリース前に完了させておくべき基盤の設定事項です。

### 1-1. メンテナンスモードの実装
システム停止時間を安全に管理するため、メンテナンスモードを導入します。
*   **環境変数設定 (Railway/Vercel)**
    *   `MAINTENANCE_MODE`: `false` (通常時) / `true` (メンテ時)
    *   `MAINTENANCE_BYPASS_TOKEN`: 開発者がアクセスするための秘密のトークン
*   **バイパスアクセス方法**
    *   `https://[アプリURL]/?preview=[トークン]` にアクセスすることで、メンテナンス中でも実機確認が可能です。

### 1-2. データ保護と監視の初期設定
*   **DBバックアップ (Supabase Pro)**: PITR（ポイントインタイムリカバリ）の有効化を確認。
*   **Google Cloud 予算アラート**: Gemini API等のコスト超過を防ぐため、予算閾値での通知を設定。
*   **重大エラー通知**: Railwayのデプロイ失敗や、サーバーの500エラーが管理者に即座に通知されるよう設定。

---

## 2. 検証環境へのデータ流し込み完了後のタスク
データの一次インポート（JSON -> 検証DB）が完了した直後に行うべき確認事項です。

### 2-1. データ整合性と品質の確認
*   [ ] **件数突合**: `check_count.js` を実行し、JSONの総件数とDBの件数が一致しているか確認。
*   [ ] **ID重複の確認**: 枝番が付与されたデータ（`-1`等）を検索し、正しいメタデータが引き継がれているかスポット確認。
*   [ ] **ファイル紐付け**: 任意の見積からPDFを開き、正しい図面・注文書が表示されるか確認。

### 2-2. AI類似検索の最終テスト
*   [ ] **ベクトル生成ステータス**: すべての図面でベクトルデータ（タイル）が生成されているかDBで確認。
*   [ ] **検索実行**: 代表的な図面で検索を行い、期待する類似図面が上位に表示されるか確認。
*   [ ] **クレジット連携**: 検索実行時に正しく1クレジットが消費されるか確認。

---

## 3. 本番リリース・DB更新の手順
安全に新機能をリリースし、検証環境からデータを同期するための標準手順です。

### 3-1. 正確な同期 (Go-live) 手順
1.  **メンテナンスモード ON**: 本番環境の `MAINTENANCE_MODE=true` を反映。
2.  **DB同期**: `pg_dump` または専用の同期ツールで、検証DBから本番DBへデータを転送。
3.  **Storage同期**: 検証Storageのファイルを本番Storageへコピー。
4.  **アプリのリデプロイ**: GitHubから最新コードを本番環境へ反映。
5.  **開発者最終確認**: バイパストークンを使用して本番URLでの疎通確認。
6.  **メンテナンスモード OFF**: `MAINTENANCE_MODE=false` に戻して公開。

### 3-2. ロールバック（切り戻し）手順
問題発生時は以下の順序で直前の状態に復旧します。
*   **アプリの差し戻し**: Railway/Vercelの履歴から一つ前の成功デプロイを再稼働（Re-deploy）。
*   **DBの差し戻し**: 用意しておいたバックアップテーブルからのデータ復元、または PITR による復旧。

---

## 4. リリース直前最終チェックリスト
Liveモード公開（メンテナンス解除）直前にチェックすべき項目です。

| カテゴリ | チェック内容 | 派生して確認すべき点 |
| :--- | :--- | :--- |
| **データ** | [ ] 商用データのインポート・同期完了<br>[ ] 既存ユーザー権限の再確認 | 古い図面データが新機能で正しく読み込めるか？ |
| **決済** | [ ] Stripe Liveモードの疎通確認<br>[ ] クレジット付与の正常性確認 | 決済失敗/中断時に適切な案内が表示されるか？ |
| **AI/UI** | [ ] 解析モーダルの全プレビュー確認<br>[ ] AIボタンのラベル・注釈の統一 | 巨大なファイルの解析時にタイムアウトが発生しないか？ |
| **法務** | [ ] 利用規約・プライバシーポリシーの最終確認 | AI解析におけるデータ利用の免責が明記されているか？ |

---

## 5. リリース後の運用・監視・コスト管理

### 5-1. 運用モニタリング計画 (Day 1)
*   [ ] **リアルタイムログ監視**: リリース直後1時間は Logtail (Railway) 等を監視。
*   [ ] **Webhook疎通監視**: Stripeからの支払い完了通知が滞りなく処理されているか確認。
*   [ ] **問合せ窓口の動作確認**: お問い合わせフォームの送受信確認。

### 5-2. AI利用状況（RPM/TPM/RPD）の監視
本番環境 (Vertex AI) と開発環境 (Google AI SDK) で監視場所が異なります。

*   **開発環境 (Google AI SDK)**: Google AI Studio (`aistudio.google.com/usage`)。サービス名「Generative Language API」。
*   **本番環境 (Vertex AI)**: Cloud Console「Vertex AI」>「割り当てとシステム上限」。
    *   `Online prediction requests per minute` (RPM)
    *   `Online prediction tokens per minute` (TPM)
*   *補足*: お支払いレポートをSKUでグループ化することでコストの詳細把握が可能。

### 5-3. 継続的な改善のベストプラクティス
*   **型変更の3段階**: `カラム追加 (Expand)` → `データ移行 (Migrate)` → `旧カラム削除 (Contract)` で無停止更新。
*   **Geminiプロンプトの管理**: スキーマ定義（DDL）をプロンプトに添え、Up/DownのSQLセットをGit管理。
*   **ストレージコスト管理**: Supabase Storageのライフサイクルポリシーを検討。
