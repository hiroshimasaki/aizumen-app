# プロジェクト引き継ぎサマリー (2026-03-10)

別の端末や新しいセッションで作業を再開するための状況まとめです。この内容を新しいエージェントに伝えることで、現在の文脈を即座に復元できます。

---

## 1. エージェントの役割と定義
- **名称**: Antigravity (Expert Developer Assistant)
- **役割**: AI図面管理システム「AiZumen」のフルスタック開発・移行支援。
- **言語方針**: 日本語（出力、コメント、ドキュメントすべて）。
- **特記**: 常に `task.md` および `task_progress.md` を最新化し、進捗を可視化する。

## 2. 現在の進捗状況 (Status: 100% Migrated)
- **データ移行**: 旧環境の商用データ（JSON & PDF）全123件を検証環境（Supabase）へ移行完了。
    - 見積データ（`quotations`）: 123件
    - ファイル実体（Storage）: 160+件
    - ベクトルデータ（`drawing_tiles`）: 100%生成（一部タイムアウトによるスキップあり）
- **直近の課題**: 移行の自動処理により、注文書などの「非図面」もベクトル化（AI解析）されてしまい、検索ノイズになっている。

## 3. 次にやるべき重要タスク (Next Actions)

### A. 高精度データクリーンアップ (最優先)
1. **AIページ判定スクリプトの実行**: 
   - `Gemini Vision` を使い、全ファイルの全ページをスキャン。
   - 「図面か」「書類か」をページ単位で判定する。
2. **インデックスの整理**:
   - 書類と判定されたページのベクトルデータを削除し、検索精度を100%に近づける。
3. **混合PDFへの対応**:
   - 1つのPDFに注文書と図面が混在していても、図面部分だけを有効にする。

### B. アプリケーション機能強化
1. **ユーザーフィードバック機能**: 
   - 検索結果でユーザーが「これは図面ではない」と指摘できるボタンの実装。
2. **検知ガードの追加**:
   - 今後の新規アップロード時、自動で図面判定を行うフローの定着。

## 4. 参照すべき主要ファイル
- **計画書**: `brain/f8a1dbba-0ce5-4a79-960a-809d000f1b97/implementation_plan.md`
- **タスク一覧**: `task_progress.md` / `docs/task_progress.md`
- **現時点の最新コード**: `feature/ai-estimation` ブランチ（または main）

---

### 新しいセッションで再開する際のプロンプト例：
> `docs/handover_summary.md` を読んで状況を把握してください。現在データ移行が123件完了した直後で、これから「AIによるページ単位の図面判定とクリーンアップ」に着手するところです。計画書（implementation_plan.md）に沿って進めてください。
